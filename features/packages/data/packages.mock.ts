import { featuredOffers } from "../../../constants/mock-data";
import type { PackagesScreenData } from "../types";

export const packagesScreenMock: PackagesScreenData = {
  highlightedPackageId: "bali-signature",
  items: featuredOffers,
  subtitle: "Destination bundles, merchandising, and premium travel packages live in one module.",
  title: "Packages",
};
